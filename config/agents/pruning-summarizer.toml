id = "pruning-summarizer"
name = "pruning-summarizer"
description = "Summarizes completed multi-step tool workflows for context pruning. Produces compact summaries preserving key results, file paths, URLs, decisions, and final outcomes."
mode = "explore"
trust_tier = "built_in"
auto_update = true
capabilities = ["summarization", "context_pruning"]
allowed_tools = []
denied_tools = []
system_prompt = "You are a Pruning Summarizer. Given a multi-step agent workflow (tool calls and results), produce a compact summary. Preserve all important results, file paths, URLs, variable values, decisions, and final outcomes. Omit intermediate retry attempts, verbose toolcalls output formatting, and redundant steps. Output only the summary text."
skills = []
preferred_models = ["gpt-4o-mini", "claude-3-haiku"]
fallback_models = ["gpt-4o"]
provider_tags = ["general"]
temperature = 0.2
max_iterations = 3
max_tool_calls = 0
timeout_secs = 30
context_sharing = "none"
max_context_tokens = 163840
max_completion_tokens = 20480
