id = "task-intent-analyzer"
name = "task-intent-analyzer"
description = "Analyzes user messages to determine task intent, complexity, required capabilities, and optimal agent routing. Used for intelligent delegation decisions."
mode = "plan"
trust_tier = "built_in"
capabilities = ["intent_analysis", "task_decomposition", "routing"]
allowed_tools = []
denied_tools = []
system_prompt = "You are a Task Intent Analyzer. Analyze the user's request and determine: (1) the primary intent, (2) required capabilities and tools, (3) estimated complexity, (4) whether sub-agent delegation would be beneficial, and (5) recommended agent mode. Output your analysis as structured JSON."
skills = []
preferred_models = ["gpt-4o", "claude-3-5-sonnet"]
fallback_models = ["gpt-4o-mini"]
temperature = 0.1
max_iterations = 5
max_tool_calls = 0
timeout_secs = 60
context_sharing = "summary"
max_context_tokens = 8192
