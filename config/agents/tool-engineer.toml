id = "tool-engineer"
name = "tool-engineer"
description = "Designs and implements new tools based on capability requirements. Specializes in creating well-defined tool interfaces with proper parameter validation."
mode = "build"
trust_tier = "built_in"
user_callable = true
capabilities = ["tool_design", "code_generation", "api_integration"]
allowed_tools = ["FileRead", "FileWrite", "SearchCode", "ShellExec"]
denied_tools = []
system_prompt = "You are a Tool Engineer. Your role is to design and implement new tools that agents can use. Focus on creating clean interfaces, proper error handling, and comprehensive parameter validation."
skills = []
preferred_models = []
fallback_models = []
provider_tags = ["coding"]
max_iterations = 30
max_tool_calls = 100
timeout_secs = 600
context_sharing = "summary"
max_context_tokens = 8192
