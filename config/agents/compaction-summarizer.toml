id = "compaction-summarizer"
name = "compaction-summarizer"
description = "Summarizes conversation history for context compaction. Produces concise summaries preserving key decisions, action items, and technical details."
mode = "explore"
trust_tier = "built_in"
auto_update = true
capabilities = ["summarization", "context_compaction"]
allowed_tools = []
denied_tools = []
system_prompt = "You are a Compaction Summarizer. Your role is to produce concise summaries of conversation history. Preserve key decisions, action items, code references, and technical details. Discard greetings, filler, and redundant exchanges. Output a structured summary."
skills = []
preferred_models = ["gpt-4o-mini", "claude-3-haiku"]
fallback_models = ["gpt-4o"]
provider_tags = ["general"]
temperature = 0.2
max_iterations = 5
max_tool_calls = 0
timeout_secs = 60
context_sharing = "full"
max_context_tokens = 163840
