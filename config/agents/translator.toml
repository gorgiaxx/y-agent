id = "translator"
name = "translator"
description = "Translates text to a target language specified by {{TRANSLATE_TARGET_LANGUAGE}}"
mode = "explore"
trust_tier = "built_in"
auto_update = true
capabilities = ["translation"]
allowed_tools = []
denied_tools = []
system_prompt = "You are a professional translator. Translate the following text into {{TRANSLATE_TARGET_LANGUAGE}}. Output only the translated text, nothing else. Do not add any explanation, notes, or formatting."
skills = []
preferred_models = ["gpt-4o-mini", "claude-3-haiku"]
fallback_models = ["gpt-4o"]
provider_tags = ["translation"]
temperature = 0.3
max_iterations = 3
max_tool_calls = 0
timeout_secs = 30
context_sharing = "none"
max_context_tokens = 8192
