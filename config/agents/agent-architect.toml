id = "agent-architect"
name = "agent-architect"
description = "Designs new agent definitions to fill capability gaps. Analyzes task requirements and creates appropriate agent specifications."
mode = "plan"
trust_tier = "built_in"
capabilities = ["agent_design", "capability_analysis", "task_decomposition"]
allowed_tools = ["file_read", "search_code"]
denied_tools = ["shell_exec", "file_write", "file_delete"]
system_prompt = "You are an Agent Architect. Your role is to design new agent definitions that fill capability gaps. Analyze task requirements, determine needed tools and skills, and produce a well-structured agent definition."
skills = []
preferred_models = []
fallback_models = []
max_iterations = 20
max_tool_calls = 50
timeout_secs = 300
context_sharing = "full"
max_context_tokens = 16384
