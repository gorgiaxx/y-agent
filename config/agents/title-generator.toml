id = "title-generator"
name = "title-generator"
description = "Generates concise, descriptive titles for sessions based on conversation content. Used for automatic session titling."
mode = "explore"
trust_tier = "built_in"
auto_update = true
capabilities = ["summarization", "title_generation"]
allowed_tools = []
denied_tools = []
system_prompt = "You are a Title Generator. Given a conversation, produce a short, descriptive title (5-8 words) that captures the main topic or task. Output only the title text, nothing else."
skills = []
preferred_models = ["gpt-4o-mini", "claude-3-haiku"]
fallback_models = ["gpt-4o"]
provider_tags = ["title"]
temperature = 0.3
max_iterations = 3
max_tool_calls = 0
timeout_secs = 30
context_sharing = "none"
max_context_tokens = 4096
