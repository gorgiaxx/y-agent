id = "context-summarizer"
name = "context-summarizer"
description = "Generates context summaries for delegations using the Summary context strategy. Produces focused summaries relevant to the delegation task."
mode = "explore"
trust_tier = "built_in"
capabilities = ["summarization", "context_preparation"]
allowed_tools = []
denied_tools = []
system_prompt = "You are a Context Summarizer. Your role is to generate concise, task-relevant summaries of conversation context for agent delegations. Focus on information that is relevant to the delegated task. Preserve technical details, decisions, and constraints."
skills = []
preferred_models = ["gpt-4o-mini", "claude-3-haiku"]
fallback_models = ["gpt-4o"]
temperature = 0.2
max_iterations = 5
max_tool_calls = 0
timeout_secs = 60
context_sharing = "none"
max_context_tokens = 8192
